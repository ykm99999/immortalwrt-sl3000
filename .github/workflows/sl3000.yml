name: 司洛SL-3000 EMMC - 修复优化版

on:
  repository_dispatch:
  workflow_dispatch:

env:
  REPO_URL: https://github.com/padavanonly/immortalwrt-mt798x-24.10
  REPO_BRANCH: 2410
  FEEDS_CONF: feeds.conf.default
  CONFIG_FILE: sl3000.config
  DIY_P1_SH: diy-part1.sh
  DIY_P2_SH: diy-part2.sh
  UPLOAD_BIN_DIR: false
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 240

    steps:
    - name: 检查工作空间
      run: |
        echo "初始磁盘空间:"
        df -h
        echo "内存信息:"
        free -h

    - name: Checkout
      uses: actions/checkout@v4

    - name: 安全深度清理
      run: |
        echo "=== 安全深度清理 ==="
        
        # 安全停止服务（忽略错误）
        sudo systemctl stop docker containerd snapd 2>/dev/null || echo "服务停止跳过"
        
        # 清理包缓存
        sudo apt-get autoremove --purge -y || true
        sudo apt-get clean || true
        
        # 清理大目录（安全方式）
        cache_dirs=(
          "/usr/share/dotnet"
          "/opt/ghc" 
          "/usr/local/lib/android"
          "/opt/hostedtoolcache/CodeQL"
          "/usr/local/lib/node_modules"
          "/var/lib/docker"
          "/var/lib/snapd"
        )
        
        for dir in "${cache_dirs[@]}"; do
          if [ -d "$dir" ]; then
            echo "清理目录: $dir"
            sudo rm -rf "$dir" 2>/dev/null || true
          fi
        done
        
        # 清理日志（不删除文件，只清空内容）
        sudo find /var/log -name "*.log" -type f -exec truncate -s 0 {} \; 2>/dev/null || true
        
        echo "清理后空间:"
        df -h
        free -h

    - name: 配置智能缓存
      uses: actions/cache@v3
      with:
        path: |
          ~/.ccache
          /workdir/openwrt/dl
          /workdir/openwrt/build_dir
        key: ${{ runner.os }}-openwrt-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-openwrt-

    - name: 克隆源码
      working-directory: /workdir
      run: |
        if [ -d "openwrt" ]; then
          echo "使用现有仓库"
          cd openwrt
          git fetch --depth=1
          git reset --hard origin/$REPO_BRANCH
        else
          echo "克隆新仓库"
          git clone --depth=1 --branch $REPO_BRANCH $REPO_URL openwrt
          cd openwrt
        fi
        
        ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

    - name: 安装必要依赖
      run: |
        echo "安装编译依赖..."
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          ccache \
          cmake \
          git \
          make \
          libssl-dev \
          libncurses5-dev \
          zlib1g-dev \
          gawk \
          gettext \
          flex \
          bison \
          python3 \
          rsync \
          unzip \
          wget

    - name: 准备编译环境
      run: |
        cd openwrt
        
        # 应用自定义配置
        [ -e $GITHUB_WORKSPACE/$FEEDS_CONF ] && cp $GITHUB_WORKSPACE/$FEEDS_CONF feeds.conf.default
        [ -e $GITHUB_WORKSPACE/files ] && cp -r $GITHUB_WORKSPACE/files .
        
        # 执行自定义脚本
        [ -x $GITHUB_WORKSPACE/$DIY_P1_SH ] && $GITHUB_WORKSPACE/$DIY_P1_SH
        
        echo "更新和安装feeds..."
        ./scripts/feeds update -a
        ./scripts/feeds install -a
        
        [ -e $GITHUB_WORKSPACE/$CONFIG_FILE ] && cp $GITHUB_WORKSPACE/$CONFIG_FILE .config
        [ -x $GITHUB_WORKSPACE/$DIY_P2_SH ] && $GITHUB_WORKSPACE/$DIY_P2_SH

    - name: 资源监控脚本
      run: |
        # 创建简单的资源监控
        cat > /tmp/monitor_resources.sh << 'EOF'
        #!/bin/bash
        echo "=== 资源监控启动 ==="
        while true; do
          echo "$(date): 内存使用: $(free -h | awk 'NR==2{print $3"/"$2}'), 磁盘使用: $(df -h /workdir | awk 'NR==2{print $3"/"$2" ("$5")"}')"
          sleep 60
        done
        EOF
        
        chmod +x /tmp/monitor_resources.sh
        # 后台运行监控
        /tmp/monitor_resources.sh &

    - name: 下载软件包
      run: |
        cd openwrt
        echo "生成配置..."
        make defconfig
        
        echo "下载软件包..."
        make download -j$(nproc) || make download -j2
        
        # 清理无效下载
        find dl -size -1024c -delete 2>/dev/null || true

    - name: 编译固件
      id: compile
      run: |
        cd openwrt
        
        echo "=== 开始编译固件 ==="
        echo "当前资源状态:"
        free -h
        df -h
        
        # 根据内存动态设置线程数
        MEM_AVAILABLE=$(free -g | awk 'NR==2{print $7}')
        if [ $MEM_AVAILABLE -lt 4 ]; then
          JOBS=1
          echo "内存紧张，使用单线程编译"
        elif [ $MEM_AVAILABLE -lt 8 ]; then
          JOBS=2
          echo "内存中等，使用2线程编译"
        else
          JOBS=$(($(nproc) - 1))
          echo "内存充足，使用$JOBS线程编译"
        fi
        
        # 开始编译
        echo "使用 $JOBS 线程编译..."
        make -j$JOBS || make -j1 V=s
        
        echo "status=success" >> $GITHUB_OUTPUT
        grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
        [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> $GITHUB_ENV
        echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV

    - name: 整理输出文件
      if: steps.compile.outputs.status == 'success' && !cancelled()
      run: |
        cd openwrt/bin/targets/*/*
        echo "=== 整理固件文件 ==="
        
        # 删除不必要的目录
        rm -rf packages 2>/dev/null || true
        
        echo "固件文件列表:"
        ls -la
        echo "FIRMWARE=$PWD" >> $GITHUB_ENV

    - name: 上传固件制品
      uses: actions/upload-artifact@v4
      if: steps.compile.outputs.status == 'success' && !cancelled()
      with:
        name: OpenWrt_固件_${{ env.FILE_DATE }}
        path: ${{ env.FIRMWARE }}/*
        retention-days: 7

    - name: 发布到GitHub Release
      if: env.UPLOAD_RELEASE == 'true' && steps.compile.outputs.status == 'success' && !cancelled()
      uses: softprops/action-gh-release@v2
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        files: ${{ env.FIRMWARE }}/*
        tag_name: build_${{ env.FILE_DATE }}
        name: 编译完成 ${{ env.FILE_DATE }}
        body: |
          🚀 OpenWrt 固件编译完成
          - 时间: ${{ env.FILE_DATE }}
          - 源码: ${{ env.REPO_URL }}
          - 分支: ${{ env.REPO_BRANCH }}
        draft: false
        prerelease: false

    - name: 清理工作空间
      if: always()
      run: |
        echo "=== 最终清理 ==="
        # 停止监控脚本
        pkill -f monitor_resources.sh 2>/dev/null || true
        echo "编译完成"
